<script>
	/** 双重for循环  外层循环一次 内层循环长度次数*/
	//自行模拟数据结构，利用数据对象的属性curd
	//substring()                                            //截取下标开始，到下标结束（不包含最后一个）
	//substr()                                              //截取下标开始，到下标结束之间的长度
	//stu.find((element) => (element.name == '李四'))      //返回该对象所有属性
	//stu.findIndex((element)=>(element.name =='李四'))   //返回数组种对应的下标
	//stu.filter(element =>  element.age>18)             //返回所有符合回调条件的元素
	//stu.map(item=>item.age+1)                         //map()返回新的数组。有返回值
	//every()和some()的方法类似&&和||                  //返回true 和false
	//indexOf()                                       //只能用于普通数组如果包含返回下标，否则返回-1
	//sort((a,b)=> return a - b )                    //用于数组排序 如果a-->b则为正序，b-->a为反序
	//replace()                             方法用于在字符串中用一些字符替换另一些字符，或替换一个与正则表达式匹配的子串
	//splice()                              接受三个参数  第一个参数从第几个开始 第二个删除个数 可选  第三个 替换要删除的位置的对象 可选
	//includes()                          //方法用来判断一个数组是否包含一个指定的值，如果是返回 true，否则false。
	//join(',')                          //转化为数组   split(',')转化为字符串  数组链式操作 最常用
	//slice(start,end)                        方法可从已有的数组中返回选定的元素。开始 --结束
	//reverse()  反转数组元素顺序
	//lastIndexOf('2')                    //寻找字符串出现的最后一个位置
</script>


